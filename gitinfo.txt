Getting Git information
create git account 
create folder (i.e shoppingApp) in D drive
verify git account
download github SCM 
verify the account then open git homepage  then create repository  i.e shopping project and choose (private or public ) repository for the git project. and successully create.
open git command (git Bash) then open folder(i.e shoppingApp) in git command 
 git command are as follows:
                             git init
							 git status
						     git add.
                             git commit-m "msg"
                             git remote add origin "url"
                             git push origin masters
                             git pull origin
                             git branch "branch name"
                             git merge origin
using the above  command							 
 
 add-stage
 commit-package
 remote-live